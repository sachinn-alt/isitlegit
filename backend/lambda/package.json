{
  "name": "isitlegit-lambda",
  "version": "1.0.0",
  "description": "IsItLegit Serverless Lambda Backend",
  "main": "scan-handler.js",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1136.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "esbuild scan-handler.ts --bundle --platform=node --target=node20 --format=esm --outfile=scan-handler.js"
  }
}
